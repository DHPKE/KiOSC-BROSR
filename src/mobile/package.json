{
  "name": "kiosc-browsr-mobile",
  "version": "2.0.0",
  "description": "KiOSC-BrowsR — Capacitor mobile app (Android / iOS)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor-community/udp": "^5.0.0",
    "capacitor-zeroconf": "^4.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "vite": "^5.0.0"
  }
}
